{
  "name": "skill-icons",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "index.js",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node build.js",
    "verify:build": "node scripts/verify-build.js",
    "deploy": "npm run build && npm run verify:build && wrangler deploy"
  },
  "devDependencies": {
    "@iconify-json/devicon": "^1.2.62",
    "@iconify-json/skill-icons": "^1.2.3",
    "wrangler": "^4.127.1"
  }
}
